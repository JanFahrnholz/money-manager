import { FC } from "react";
import Contact from "../../types/Contact";

type Props = {
    contact: Contact;
};

const StampcardProcessWidget: FC<Props> = ({ contact }) => {
    return <>TODO</>;
};

export default StampcardProcessWidget;
